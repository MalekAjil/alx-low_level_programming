#include <main.h>
#include "string.h"

/**
 * _atoi - return integer from string
 * @s: the string
 * Return: integer number
 */
int _atoi(char *s)
{
	int i, j, nb;

	j = strlen(s);
	nb = atoi(s);
	return (nb);
}

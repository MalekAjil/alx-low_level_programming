#include "main.h"
#include "string.h"
#include <stdlib.h>

/**
 * _atoi - return integer from string
 * @s: the string
 * Return: integer number
 */
int _atoi(char *s)
{
	int nb;

	nb = atoi(s);
	return (nb);
}
